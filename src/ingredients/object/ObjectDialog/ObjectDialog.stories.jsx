import React from 'react'

import ObjectDialog from './ObjectDialog'

export default {
  title: 'Ingredients/Object/Dialog',
  component: ObjectDialog,
}

const Template = (args) => <ObjectDialog {...args} />


export const Basic = Template.bind({})
Basic.args = {
}
